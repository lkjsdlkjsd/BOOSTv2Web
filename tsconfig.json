{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  "compilerOptions": {
    "noEmitOnError": false, // This allows the build even if there are type errors
    "strict": false, // Disable strict type checking if needed
    "skipLibCheck": true // Skip type checking for declaration files
  }
}
